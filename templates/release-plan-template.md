# {{项目名称}} 发布方案

## 0. 版本与评审

| 方案版本 | 日期 | 目标环境 | 候选版本 | 变更原因 | 评审状态 | Reviewer |
|----------|------|----------|----------|----------|----------|----------|
| v0.1 | YYYY-MM-DD | 预发/生产 | commit/tag | 待定 | awaiting-review | 待定 |

评审状态只能为 `awaiting-review`、`approved` 或 `rejected`。系统范围、候选 commit、迁移、环境、顺序、alias 或构建映射变化后必须重新评审。发布状态按 `discovered -> impact-mapped -> release-plan-approved -> candidate-frozen -> preview-verified -> default-branch-merged -> production-deployed -> post-release-reconciled -> worktrees-synchronized -> complete` 推进；任一门禁失败时保持 `blocked` 或 `partially-released`。

## 1. 发布目标与范围

- 用户结果：
- 本次变更：
- 明确不在范围：
- 成功标准：
- 发布完整性参考：`references/release-integrity.md`

## 2. 调用链与关联系统盘点

沿真实调用链检查：客户端 -> 主服务 -> Auth/AI/邮件/Worker/Cron -> 数据库/对象存储/第三方；同时检查管理后台、移动端和独立任务。

- Runtime entry inventory 路径/版本：
- 本次登录入口：
- 本次主服务/BFF URL：
- 本次 provider callback URL：
- 本次业务回跳 URL：
- callback/fixed alias inspect 结果：

### 2.1 双向影响闭包

- 入口向下游扫描来源（runtime entry inventory、架构调用图、部署配置、环境变量清单、API/事件契约）：
- 候选仓库与 `base...head` 变更扫描命令：
- 反向扫描的代码调用/API/事件/队列/Cron/Webhook/共享包/迁移/配置结果：
- 下游回到用户入口的反向复核结果：
- 未确认的 owner、调用方向、兼容性或部署目标：无/阻断原因

每一行发布决策只能填写：`发布`、`不发布（证据）`、`本次已在前置版本发布` 或 `阻断`。空白、`待定` 或“依赖上游”不是有效决策。

| 序号 | 系统/组件 | 仓库 | 默认分支 | 云项目/运行平台 | 固定域名/入口 | 当前 deployment | 实现/head commit | 默认分支 merge commit | deployment/产物 commit | 已归并默认分支 | 发布决策 | 判断依据 | 上下游依赖 |
|------|-----------|------|----------|---------------|---------------|--------------------|------------------|----------------------|------------------------|------------------|----------|----------|------------|
| 1 |  |  | main/master |  |  |  |  | 待合并/commit | 待部署/commit | 是/否/不适用 | 发布/不发布 |  |  |

从用户入口沿主服务继续递归盘点内部依赖，所有关联系统都必须出现；客户端不直连内部服务不能作为排除证据。没有代码或配置变化的系统也必须填写“不发布”及证据，禁止省略。

### 2.2 变更来源与发布决策证据

| 系统/仓库 | base commit | head/候选 commit | 代码/API/事件/配置变化 | 依赖或被依赖对象 | 发布决策 | 证据路径/命令 | Owner |
|-----------|-------------|------------------|------------------------|------------------|----------|---------------|-------|
|  |  |  |  |  | 发布/不发布（证据）/前置版本/阻断 |  |  |

## 3. 环境、数据与兼容性

| 系统 | 环境变量/密钥检查 | 数据库 host/schema | Migration/回填 | API/事件兼容 | 缓存/队列/对象存储 | 结论 |
|------|-------------------|--------------------|----------------|-------------|--------------------|------|
|  |  |  | 无/命令与回滚 |  |  |  |

## 4. 发布顺序与退出条件

| 顺序 | 系统 | 发布动作 | 前置条件 | 发布后验证 | 观察时长/指标 | 失败动作 |
|------|------|----------|----------|------------|---------------|----------|
| 1 |  |  |  |  |  | 停止/回滚 |

任何一步未达到退出条件时停止后续发布和流量切换。

Production 默认按依赖优先顺序发布：先数据/内部服务，再主服务，最后客户端或流量开关。客户端包、灰度包或商店构建可以先产出，但不能在依赖服务、默认分支和跨系统契约核销前把完整 Release Train 标记为 `complete`。

## 5. 测试、监控与证据

- 预发 deployment：
- 测试报告：
- Code Review/PR：
- 线上 smoke：
- 线上回归：固定生产域名、受控生产账号、P0 核心路径、关联系统契约、相邻高风险路径、失败路径。
- 低流量合成探针：探针路径、轮次、期望状态、禁止副作用和凭据保护；不适用时说明原因。
- DB after/副作用：行数、owner、状态迁移、队列/对象存储变化；只记录脱敏聚合结果。
- 日志观察窗口：自然流量不足时不能用 `0` 错误代替合成探针。
- Alias/deployment 漂移：逐个固定 alias 和 provider callback alias inspect，记录实际 project/deployment/commit/target。
- 回归失败动作：P0/P1 失败立即停止核销并按系统回滚；修复后重新经过预发、评审、生产部署和回归。
- 监控指标：5xx、延迟、登录失败、AI 失败、任务积压、数据库错误。
- 证据路径：

## 6. 回滚方案

| 回滚顺序 | 系统 | 上一稳定版本/deployment | 回滚命令或平台动作 | 数据回滚 | 回滚后验证 | Owner |
|----------|------|-------------------------|--------------------|----------|------------|-------|
| 1 |  |  |  | 不适用/步骤 |  |  |

## 7. 发布前评审结论

- 范围完整性：通过/不通过
- 候选版本可追溯：通过/不通过
- Code PR 已归并默认分支：通过/不通过/仅 Preview 不适用
- Production deployment/产物与默认分支 merge commit 一致：通过/不通过/仅 Preview 不适用
- 环境隔离：通过/不通过
- 数据与迁移：通过/不通过
- 测试和 CR：通过/不通过
- 回滚可执行：通过/不通过
- 双向影响闭包完成：通过/不通过
- 每个关联系统均有发布决策和证据：通过/不通过
- 候选已冻结且变更后会重新审批：通过/不通过
- 最终状态：`awaiting-review`
- 审批人和时间：

## 8. 发布后核销

### 8.1 最终证据门禁

| 证据 | 结果 | 证据路径/摘要 | 不适用理由 |
|------|------|---------------|------------|
| 真实用户或受控真实账号验证 | 待核销 |  |  |
| 低流量合成探针 | 待核销 |  |  |
| DB after/副作用检查 | 待核销 |  |  |
| 日志观察窗口 | 待核销 |  |  |
| Callback/fixed alias 漂移检查 | 待核销 |  |  |

| 系统 | 计划决策 | 默认分支 merge commit | 实际版本/deployment | deployment/产物 commit | 固定域名 | Smoke | 线上回归 | 监控/观察窗口 | 回滚点 | 核销结果 |
|------|----------|----------------------|---------------------|------------------------|----------|-------|----------|---------------|--------|----------|
|  |  |  |  |  |  |  |  |  |  | 待核销 |

- 是否存在漏发系统：
- 是否发生方案漂移：
- 线上回归结果：
- 残余风险：
- 客户端/单系统产物状态：不得代替完整 Release Train 状态。
- 最终发布结论：只有全部系统核销、默认分支与 deployment commit 可追溯、线上回归通过且观察窗口完成后才能填写 `complete`。

### 8.2 完整性检查

- `scripts/check_release_integrity.sh` 命令与输出摘要：
- 候选 commit 已被远端默认分支包含：通过/不通过
- deployment 源码 commit 与候选/merge commit 一致：通过/不通过
- 默认分支 worktree 与远端 HEAD 一致且干净：通过/不通过
- 其它 worktree 的落后、脏状态及保存/同步动作：
- 发现漏发或版本漂移时的阻断/回滚/重新审批记录：

| 仓库 | Worktree 路径 | 分支 | 默认分支 | 候选 merge commit 已在远端 | deployment/产物 commit | `git pull --ff-only` | 本地/远端 HEAD | 工作区状态 | 归位结果 |
|------|---------------|------|----------|---------------------------|------------------------|-----------------------|---------------|------------|----------|
|  |  |  |  | 是/否 |  | 通过/失败/不适用 |  | clean/dirty | 待核销 |
