# {{项目名称}} 发布方案

## 0. 版本与评审

| 方案版本 | 日期 | 目标环境 | 候选版本 | 变更原因 | 评审状态 | Reviewer |
|----------|------|----------|----------|----------|----------|----------|
| v0.1 | YYYY-MM-DD | 预发/生产 | commit/tag | 待定 | awaiting-review | 待定 |

评审状态只能为 `awaiting-review`、`approved` 或 `rejected`。系统范围、候选 commit、迁移、环境或顺序变化后必须重新评审。

## 1. 发布目标与范围

- 用户结果：
- 本次变更：
- 明确不在范围：
- 成功标准：

## 2. 调用链与关联系统盘点

沿真实调用链检查：客户端 -> 主服务 -> Auth/AI/邮件/Worker/Cron -> 数据库/对象存储/第三方；同时检查管理后台、移动端和独立任务。

- Runtime entry inventory 路径/版本：
- 本次登录入口：
- 本次主服务/BFF URL：
- 本次 provider callback URL：
- 本次业务回跳 URL：
- callback/fixed alias inspect 结果：

| 序号 | 系统/组件 | 仓库 | 云项目/运行平台 | 固定域名/入口 | callback/业务回跳 | 当前 deployment | 候选 commit/tag | 发布决策 | 判断依据 | 上下游依赖 |
|------|-----------|------|---------------|---------------|-------------------|--------------------|-----------------|----------|----------|------------|
| 1 |  |  |  |  |  |  |  | 发布/不发布 |  |  |

所有关联系统都必须出现。没有代码或配置变化的系统也必须填写“不发布”及证据，禁止省略。

## 3. 环境、数据与兼容性

| 系统 | 环境变量/密钥检查 | 数据库 host/schema | Migration/回填 | API/事件兼容 | 缓存/队列/对象存储 | 结论 |
|------|-------------------|--------------------|----------------|-------------|--------------------|------|
|  |  |  | 无/命令与回滚 |  |  |  |

## 4. 发布顺序与退出条件

| 顺序 | 系统 | 发布动作 | 前置条件 | 发布后验证 | 观察时长/指标 | 失败动作 |
|------|------|----------|----------|------------|---------------|----------|
| 1 |  |  |  |  |  | 停止/回滚 |

任何一步未达到退出条件时停止后续发布和流量切换。

## 5. 测试、监控与证据

- 预发 deployment：
- 测试报告：
- Code Review/PR：
- 线上 smoke：
- 线上回归：固定生产域名、受控生产账号、P0 核心路径、关联系统契约、相邻高风险路径、失败路径。
- 低流量合成探针：探针路径、轮次、期望状态、禁止副作用和凭据保护；不适用时说明原因。
- DB after/副作用：行数、owner、状态迁移、队列/对象存储变化；只记录脱敏聚合结果。
- 日志观察窗口：自然流量不足时不能用 `0` 错误代替合成探针。
- Alias/deployment 漂移：逐个固定 alias 和 provider callback alias inspect，记录实际 project/deployment/commit/target。
- 回归失败动作：P0/P1 失败立即停止核销并按系统回滚；修复后重新经过预发、评审、生产部署和回归。
- 监控指标：5xx、延迟、登录失败、AI 失败、任务积压、数据库错误。
- 证据路径：

## 6. 回滚方案

| 回滚顺序 | 系统 | 上一稳定版本/deployment | 回滚命令或平台动作 | 数据回滚 | 回滚后验证 | Owner |
|----------|------|-------------------------|--------------------|----------|------------|-------|
| 1 |  |  |  | 不适用/步骤 |  |  |

## 7. 发布前评审结论

- 范围完整性：通过/不通过
- 候选版本可追溯：通过/不通过
- 环境隔离：通过/不通过
- 数据与迁移：通过/不通过
- 测试和 CR：通过/不通过
- 回滚可执行：通过/不通过
- 最终状态：`awaiting-review`
- 审批人和时间：

## 8. 发布后核销

### 8.1 最终证据门禁

| 证据 | 结果 | 证据路径/摘要 | 不适用理由 |
|------|------|---------------|------------|
| 真实用户或受控真实账号验证 | 待核销 |  |  |
| 低流量合成探针 | 待核销 |  |  |
| DB after/副作用检查 | 待核销 |  |  |
| 日志观察窗口 | 待核销 |  |  |
| Callback/fixed alias 漂移检查 | 待核销 |  |  |

| 系统 | 计划决策 | 实际版本/deployment | 固定域名 | Smoke | 线上回归 | 监控/观察窗口 | 回滚点 | 核销结果 |
|------|----------|---------------------|----------|-------|----------|---------------|--------|----------|
|  |  |  |  |  |  |  |  | 待核销 |

- 是否存在漏发系统：
- 是否发生方案漂移：
- 线上回归结果：
- 残余风险：
- 最终发布结论：只有全部系统核销、线上回归通过且观察窗口完成后才能填写 `complete`。
- 发布后仓库归位：记录受影响仓库的默认主分支、`git pull --ff-only` 结果、本地/远端 HEAD 和工作区状态。
